{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@now/node",
      "config": {
        "helpers": false
      }
    }
  ],
  "routes": [{ "src": "/.*", "dest": "/api/index.ts" }]
}
